global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  # - "first_rules.yml"
  # - "second_rules.yml"

scrape_configs:
  # The job name is added as a label `job=<job_name>` to any timeseries scraped from this config.
  - job_name: "prometheus"
    static_configs:
      - targets: ["localhost:9090"]

  # Currency API metrics - scrape both replicas
  - job_name: "currency-api"
    static_configs:
      - targets: ["demo_currency_app-api-1:8000", "demo_currency_app-api-2:8000"]
    metrics_path: "/metrics"
    scrape_interval: 5s
    scrape_timeout: 5s
    scheme: http

  # Additional services can be added here as needed

  # cAdvisor container metrics (ARM64 compatible)
  - job_name: "cadvisor"
    static_configs:
      - targets: ["cadvisor:8080"]
    metrics_path: "/metrics"
    scrape_interval: 15s
    scrape_timeout: 10s
    scheme: http
